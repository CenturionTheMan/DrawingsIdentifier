<UserControl x:Class="DrawingIdentifierGui.Views.Windows.NeuralNetworkLearning"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="clr-namespace:DrawingIdentifierGui.Views.Controls"
             xmlns:windows="clr-namespace:DrawingIdentifierGui.ViewModels.Windows"
             mc:Ignorable="d"
             d:DesignHeight="550" d:DesignWidth="800">
    <Border CornerRadius="15 0 15 0"
            Background="{StaticResource colorLight2}"
            BorderBrush="{StaticResource colorDark1}"
            BorderThickness="1">

        <Grid MinHeight="550" MinWidth="700">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.5*" />
                <ColumnDefinition Width="0.5*" />
            </Grid.ColumnDefinitions>

            <controls:SingleLearningComponent Grid.Column="0" x:Name="Neural_Network"
                                              Margin="10 10 0 10" />
            <controls:SingleLearningComponent Grid.Column="1" x:Name="Convolutional_Neural_Network"
                                              Margin="0 10 10 10" />
        </Grid>
    </Border>
</UserControl>